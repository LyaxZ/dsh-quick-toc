# 更新日志

**dsh-quick-toc** 的重要变更都记录在这里。英文版见 [CHANGELOG.en.md](CHANGELOG.en.md)。

## [0.7.6] - 2026-09-24

### 修复
- **0.1.7 线上设置卡片的写入只落在浏览器里**：入口 id 与候选名不一致的 profile 上（例如真实 profile 把入口命名为 `dsh-quick-toc`，而候选表第一个是 `quick-toc`），插件加载的那一刻 `configForms.describe()` 的视图**还没到**（实测返回空表），于是找设置命名空间的那段代码按候选名猜了一个**宿主并不服务**的名字，绑上一个永远不就绪的表单——卡片照常能开能点，但所有写入都退回本浏览器的镜像，设置文档（0.1.7 上是 profile 的 `cordis.patch.yml`）永远不动，而且控制台没有任何报错。现在：视图为空时**不再猜名字**（空 ≠ 宿主不服务），改为订阅 `describe()` 并在文档到位后绑定，绑定成功才注册卡片；宿主始终不给文档时 4 秒后仍注册卡片、退回本地镜像这个既有的降级层。0.1.7-rc.1 与 0.1.5-rc.3 两条线都实测确认写入落盘（前者 `cordis.patch.yml` 新增 `lang: en`，后者 `settings.yaml` 的 `dsh-quick-toc:` 变成 `{ handle: 0.6, autoLoad: false, lang: en }`）。

## [0.7.5] - 2026-09-24

### 变更
- **支持 DSH 0.1.7-rc.1**：它是 0.1.7 那套接口搬到 rc 通道（`settingsScope`、`settings.plugin.item`、`sessions.open`、`installSection` 都已不存在，与 0.1.7-alpha.x 共用 `configForms`、`plugins.bundle.config`、`uiWorkspace`、`settings.configure`），代码不需要改动。隔离环境实测：宿主启动无报错，面板、幕布、Esc、停靠切换与收起在一段真实会话上逐项通过，浏览器无报错。`dsh.compatibility.dshReleases` 随之增加该版本。
- **声明的兼容下限收到 0.1.5-rc.3**：更早的 0.1.5-rc.1、0.1.5-rc.2 不再声明（`engines.dsh` 与两条 peer 区间同步收紧），README 兼容表跟上。
- **插件自带的 loader 行 id 与设置命名空间对齐**（`quick-toc` → `dsh-quick-toc`）：DSH 0.1.7 起设置文档改为按**入口 id** 寻址的表单，旧 `settings.yaml` 被改名为导入源、按同名把旧配置映射到入口；id 与插件注册的命名空间不一致时，已保存的偏好会被留在导入文件里（插件读到空值）。同名之后本插件可以放进 profile 的 `dsh.profile.bundles`（0.1.7 的插件管理页只在 bundles 里的包上给出配置页），0.1.5-rc.3 上 id 只是加载器键、同样无害。**装进 `bundles` 与 profile patch 的 `insert:` 不能同时存在**（同名 entry id 会让宿主直接启动失败）。

### 修复
- **设置作用域的绑定兜底**：宿主设置文档在插件加载时可能仍在载入（0.1.7-rc.1 实测该表单的 `status` 为 `loading`），原先只认一次「就绪」快照；现在会退到第一个存在的候选表单，由订阅在文档到位后接续。

## [0.7.4] - 2026-09-23

### 修复
- **长会话里打开幕布没有展开动画**：打开幕布时，本插件自己的自动加载把整段会话一次性拉了进来（实测 DOM 节点 1365 → 46150、单帧长任务 275–897ms），0.46 秒的展开动画在第一帧画出来之前就播完了。自动加载现在必须由读者的真实操作触发（列表上的滚轮、面板上的按键、在列表上按下），几何上的巧合（短列表恰好装得下、刚打开幕布、窗口改变尺寸）不再被当成加载请求。
- **切换停靠边时面板先消失、再从另一边滑出来**：切换原本移动 `left`，而 `left` 不在过渡列表里——盒子瞬移，只有位移在补间。面板现在始终停在一个固定锚点上，展开、收起、两个停靠边、交给幕布这四个位置都是锚点上的位移，切换停靠边是一段纯位移动画。
- **收起后停的位置比展开位置偏左（左停靠实测 8px）**：收起的裁剪量原本是面板宽度（左停靠另加 8px），与盒子真正滑动的距离（左停靠为面板宽度 + 16px）不相等，可见边一路漂移。裁剪量改为与实际滑动距离一致后，可见边全程钉在面板自己那条开合线上，左右停靠对称。
- **收起时偶尔收偏**：宿主会在收起动画途中重新测量会话区，目标线随之改变（实测收进去的位置从 1526 跑到 1396）。收起开始的那一步现在冻结本次动画使用的几何，之后再展开才读取新几何。

## [0.7.3] - 2026-09-23

### 变更
- **兼容 DSH 0.1.5-rc.3 与 0.1.7-alpha.2**：两个新版本上逐项实测通过（面板与幕布、点行跳转与翻历史、标题/全文/跨会话三档检索、跨会话命中切换会话、设置卡片与设置文档的读写），代码无需改动；兼容声明与 README 兼容表已随之更新。
- 文档里对两条宿主线的实现说明补齐：设置服务与配置卡片槽位各写清 rc 线与 alpha 线两套（此前只写了 rc 线那一套）。

## [0.7.2] - 2026-09-22

### 新增
- **幕布缩放**：设置卡片新增「幕布缩放」滑块（50%–200%、每档 5%），把幕布里的文字、图标与按钮按比例缩放，幕布本身的尺寸不变；与「面板缩放」互相独立。幕布内容的默认大小比改动前小一档——滑块的 100% 即改动前的 90%。
- **支持 DSH 0.1.7-alpha.1**：同一份安装包在 0.1.5-rc.x 与 0.1.7-alpha.x 上都能用（设置接口、配置卡片槽位与会话切换各按宿主实际提供的那一套走），0.1.5-rc.2 与 0.1.7-alpha.1 上都实测通过。

## [0.7.1] - 2026-09-19

### 新增
- **「回到我滚到的位置」浮动按钮**：关闭面板或幕布时记录列表顶部的那一行，再次打开时在右上角显示向上的按钮，可回到该行原来的位置。两个界面分别记录；使用一次或自行滚回该处后按钮消失，直到下次关闭重新记录。

### 变更
- **打开面板或幕布时，正在阅读的那一行落在列表顶部**（原先只有幕布如此，面板显示上次停留的位置）。
- **面板的收起/展开改为位移过渡**，不再逐帧重排整个面板，长会话中的收起动画不再变形。
- **顶部图标的几何与线宽调整**：幕布钮改为横矩形、箭头朝下；层级筛选与「只看提问」的线条加粗；✕ 与放大镜不再用平移微调位置。
- 设置卡片中「记住阅读位置」「自动加载历史」两行不再显示说明文字。

### 修复
- **自动加载速度较慢**：视野中出现未加载的占位行时改为一次加载到位，只在接近预取范围时逐页加载；并修正把「正在加载」判断为「已经是最早的消息」的误报。
- **加载历史记录时向上滚动被拉回**：高度补偿改为与内容变化同帧完成，锚点取视野中第一个已加载的行，宿主自身翻页时同样补偿。
- **使用一段时间后出现卡顿**：自动跟随不再对会话中的每条消息逐个测量高度。
- **自动加载历史时中间的对话向上移动**：翻页前不再抬升对话区。
- **一次向上滚动会连续加载多页**：预取判断改为从视野中最近的占位行起算，并加入迟滞。
- **幕布展开时列表先停在偏上的位置、向上跳转的落点不准、打开时先自上而下滚动**：统一为「正在阅读的那一行对齐列表顶部、瞬时完成」。
- **顶部液滴下压到底后仍留有一线厚度，随后整体消失**：改为压到底即完全没入分割线。

## [0.7.0] - 2026-09-19

### 新增
- **全屏「幕布」大纲**：对话顶边的标签条中嵌入一枚液滴把手，点击后大纲以整幅宽度从标签条下方展开，高度取对话区的剩余部分、下沿圆角，不遮挡也不压暗下方的对话。点击任意条目、按 Esc 或点右上角 ✕ 收起；面板标题栏的「幕布」圆钮是另一个入口。它是把面板本体搬入容器，因此翻页、搜索、悬停预览、键盘光标与阅读位置都是同一套。
- **幕布中的宽版排版**：一行三段——层级徽标、更大的标题、该节正文的开头；层级筛选排成一行。点击放大镜，搜索栏从右侧展开。
- **每行末尾的「跳到本节末尾」按钮**：指针停在标题行、组头或搜索结果上时，右端显示圆钮，点击跳到该节的结束位置（标题行停在下一个同级或更高级标题之前，组头停在整轮对话的末尾）。
- **跨会话检索**：搜索范围增加第三档「会话」，用宿主的全文索引搜索其他会话的正文（只搜正文，一次最多 20 条），点击结果切换到该会话并继续查找。无法打开的会话会被略过并报出条数，宿主未开启索引时直接说明。
- **记住每会话阅读位置**（默认开启）：重新打开会话时回到上次读到的那一轮；记录带 seq，该轮不在对话窗口中时也会自动加载。位置只保存在本机，超过半小时不再恢复。
- **键盘导航**：↑/↓ 在大纲中移动光标，Enter 激活当前行，Home / End 到首尾，Esc 收起面板；点击面板任意位置后即可使用，不必先 Tab 进入。
- **只看提问**：标题栏的语音气泡按钮把大纲折叠为每轮的时间与提问首行；切换为渐隐渐显，开启时层级筛选不可用。

### 变更
- **收起后的边缘把手改为沿边线滑入**：展开时把手先淡出、面板随后滑入；收起时把手沿边线滑回并被裁切。

### 修复
- **面板不再覆盖其他插件的浮层**：层级改为插入应用层叠序中的空档（把手 12、面板 14、幕布 16、悬停预览 18），仍在对话内容之上，但在宿主与其他插件的浮层之下。

## [0.6.3] - 2026-09-15

### 修复
- **配置卡片的展开箭头与宿主不一致**：原先使用文本字符 `⌄`，比宿主的 14×14 图标更细，旋转时位置偏移；现改为与宿主相同的内联 SVG 箭头，绕自身中心翻转。

## [0.6.2] - 2026-09-15

### 新增
- **收起把手的垂直位置**：设置卡片新增滑块（0%–100%、每档 1%），决定面板收起后把手在对话区中的高度：0% 在最下、100% 在最上，默认 50%。

### 变更
- **写入默认值等于「未自定义」**：卡片中的字段回到默认值后会从设置的用户层移除，「已自定义」与「重置」标记随之消失。

### 修复
- **面板尺寸有时无法保存**：尺寸写入有 0.4 秒去抖，而台账在发起写入时即被标记——拖动后 0.4 秒内再次调整会取消前一次的写入，该尺寸此后不再保存。现在台账只在真正写入时标记。
- **偏小的面板尺寸无法保存**：读回的下限（180×160）与拖动的下限（120×60）不一致，现统一为 120×60。
- **高度为「自动」时第一次拖动会使面板跳动**：拖拽起点改为面板的实测高度。

## [0.6.1] - 2026-09-15

### 新增
- **面板缩放滑块**：设置卡片新增「面板缩放」（50%–200%、每档 5%），把面板内容等比缩放，面板本身的尺寸不变。拖动时只更新读数，松手后写入设置。
- **窄面板的顶栏**：面板变窄时四颗按钮保持在一行——先压缩中间的空白，挤满后整行可横向滚动，顶部灰色拖拽条随之渐隐。

### 变更
- **跳转动画改为浏览器自身的平滑滚动**（按真实时间推进，60Hz 与 240Hz 屏幕上时长一致）；目标在滚动过程中移位或动画被打断时会重新瞄准，仅当浏览器把该调用执行为瞬移时才改用逐帧绘制。
- **面板尺寸取消上下限**：只保留仍能拖回的 120px 宽 / 60px 高下限。

### 修复
- **长距离跳转在 240Hz 屏幕上过快**：原动画按帧数计时，240Hz 下仅约 190ms。现在按真实时间推进，与刷新率无关。
- **拖动面板尺寸后会自动回到原尺寸**：面板把「传出改动」与「采纳外部改动」记录在同一份台账中，拖动期间一旦有设置写入，就会用尚未保存的旧值把面板拉回。现在两条路径分开记录。

## [0.6.0] - 2026-09-14

### 新增
- **界面中英双语**：语言设置可选 `跟随宿主`（默认）、`中文`、`English`；中文时间戳保留「昨天 / 前天」，英文使用 `yesterday` 与日期形式。
- **插件配置卡片**：**设置 → 插件 → 插件配置** 中的「对话大纲」卡片集中管理语言、默认停靠边缘、显示的标题层级、模糊搜索、悬停预览与诊断开关；改动过的字段标有「已自定义」并支持单独重置。
- **面板与卡片实时同步**：两者读写同一份偏好，任一侧改动后另一侧立即生效，无需刷新。
- **偏好保存到 DSH 设置**：偏好写入宿主设置文档（跟随配置），浏览器本地保留一份镜像供无法写入设置的页面使用；0.5.x 的本地值在宿主层首次应答时导入一次。

### 变更
- **长距离跳转恢复动画**：由插件逐帧绘制（按时长而非帧数），每帧重新瞄准目标、落点后短暂复检，跨页远跳不再半途停止。
- **面板拉高后大纲自动填满**，最新一组下方不再留白。
- **「正在阅读」蓝框的渐显渐隐时间加长到 0.4 秒**。
- **顶边距、宽度、高度移出设置**，改为只能拖拽调整。
- **偏好写入合并**：不再在指针移动时反复写入存储，改为合并后延迟写入；诊断开关进入设置卡片，打开后立即打印。

### 修复
- **面板在部分会话中崩溃（React #310）**：渲染路径上的 hook 数量不一致，面板连同槽位一起消失。现在拆分为不调用 hook 的外层守卫与内层组件。

## [0.5.1] - 2026-09-13

### 新增
- **「回到底部」浮动按钮**：列表不在底部时显示，点击回到最新一条，到底后隐藏。
- **回合时间带日期**：昨天、前天分别显示「昨天 / 前天 + 时间」，更早显示 `YY-MM-DD HH:MM`（按自然日判定）。
- **失败回合的报错**：请求超时、上游报错等回合在组头下显示 `请求失败` 与报错原文，点击跳到该位置，报错原文也可被搜索命中。

### 变更
- **标题栏改为一边两个按钮**（左：层级筛选、停靠切换；右：搜索、收起），并移除左侧的三条杠。
- **层级筛选弹层改为左对齐按钮**。
- **挂载诊断日志默认静默**，需要时把 `dsh-quick-toc.debug` 设为 `1` 后刷新。
- **长距离跳转改为即时定位**，落地后进行短暂校验。

### 修复
- **失败或中止的回合被误标为「未加载」**：现在这些回合按已加载处理，失败回合额外显示报错行。
- **从大纲向上加载更早消息时，对话区会回到最新一条**：翻页前先把对话区移出贴底区域。
- **无标题回合的组头格式与其他组头不一致**：现在共用同一套样式。

## [0.5.0] - 2026-09-12

### 新增
- **全量回合索引**：接入宿主投影后大纲覆盖整段会话，未加载的回合带「未加载」标记与宿主提供的预览，点击后加载该回合并滚动过去。
- **标题行副标题**：每个标题下方显示该节正文的第一句，便于区分同名标题。
- **悬停预览**：指针停在标题行约 0.26 秒后显示预览卡片（标题、时间、层级路径、该节开头）；面板停靠右侧时显示在左侧。
- **搜索归一化（常开）**：大小写、全角半角与空白差异视为同一个匹配。
- **「模糊」开关**：允许关键字中间夹少量其他文字，状态会被记住。
- **未加载的回合也可被搜索到**：全文范围同时搜索这些回合的预览文本。
- **边界提示**：搜索结果下方提示可加载更早的消息；滚动到最早或最后仍继续滚动时，面板底部显示提示，停留约 2.6 秒。

### 变更
- **自动跟随更及时**：移除人为延时、改为瞬时定位、缩短高亮过渡；跟随不再依赖挂载时获取的滚动容器，改为在 document 捕获阶段监听并在每次运行时重新查询。
- **正在阅读的回合改用封闭蓝框**标记，其余回合的暗度减弱，面板静止透明度提高到 0.72。
- 无标题回合的时间行提升为正文级字号与字重。
- 界面激活态配色统一到单一色源。
- 向上滚动时的「加载更早」加入节流，避免连续触发分页。
- 界面文案统一到一张字符串表，为后续双语做准备。

### 修复
- **用对话右侧的回合轨道跳转后，大纲不再跟随、也不再点亮**（滚动监听绑定在被替换的容器上）：改为在 document 捕获阶段监听并重新查询容器。
- **没有 Markdown 标题的回合不会被标记为「正在阅读」**（映射只登记了带标题的消息）：现在该回合的所有节点都会登记。

## [0.4.0] - 2026-09-11

### 新增
- **搜索结果列表**：有搜索词时大纲切换为结果列表（标题高亮、层级路径、回合时间、重复次数，全文模式附片段），最新的命中在底部，点击任一行即设为当前命中并在对话中高亮滚动；`n/N` 回车逐处跳保留
- **组头吸顶**：当前回合的组头固定在面板顶部
- **层级筛选行可收起**：标题栏的「层级」按钮收起/展开 `1`–`6` 那一行
- **位置面包屑**：面板顶部显示正在阅读的章节路径，点击跳到该标题
- **标题层级筛选**：`1`–`6` 芯片各自独立，可任意组合；全部关闭时恢复显示六级

### 变更
- **性能**：标题解析与文本提取按节点缓存，流式更新只重算真正变化的节点
- 组头（时间行）点击改为跳到该回合模型回答的开头
- 声明兼容 DSH `0.1.5-rc.1` 与 `0.1.5-rc.2`
- `countOccurrences` 增加空查询守卫

### 修复
- **首次载入没有定位到最新回合**：副作用的依赖写在声明之前，只执行了一次
- **拖动顶部横条后记录的位置是拖动前的**，刷新后面板回到原位
- **同一文本节点中的第二处命中不被高亮**，跳转到该处时只滚动、不高亮
- **围栏代码块中的 `#` 行被当作标题**，使其后真标题的序号错位

## [0.4.1] - 2026-09-12

### 变更
- **层级筛选改为弹出选择**：标题栏新增圆形按钮，点击后从按钮中心弹出 H1–H6 选择层，收回时缩回并渐隐，不再单独占一行
- 移除面板顶部的位置面包屑行（0.4.0 新增）

## [0.3.3] - 2026-09-10

### 新增
- 中间列切换到其他视图时，大纲面板与边缘把手渐隐，切回对话视图时渐显

### 变更
- 兼容性只声明 DSH `0.1.5-rc.1`
- README 补充搜索与跳转相关说明；兼容表列出最近两个版本各自支持的 DSH 版本
- CHANGELOG 拆分为中英两份；Release 描述默认取中文段，英文段折叠在下方

## [0.3.2] - 2026-09-08

### 变更
- 文档整理：README 的兼容表与功能列表更新，npm 描述补充关键字搜索与对话内高亮

## [0.3.1] - 2026-09-08

### 修复
- 面板与收起把手不再浮在 DSH 弹层之上（z-index 改为 500）
- 圆形控件在 DSH 0.1.5-rc.1 上恢复为正圆：该主题的 `corner-shape` 会把 50% 圆角变为方圆角

### 变更
- 兼容声明加入 DSH `0.1.5-rc.1`

## [0.3.0] - 2026-08-24

### 修复
- 适配 DSH 0.1.2-rc.1：不再因 `dsh-client-runtime` 被移除而加载失败，运行时 hook 改由会话作用域槽位的 props 注入
- 对话数据访问由 `useSession` 改为会话作用域的 `useChat`
- 面板挂载改到会话作用域的 `conversation.input.overlay` 槽，解决子槽始终空渲染

### 变更
- `dsh.client.inject` 改为列出 `dsh-client-ui-chat` / `-conversation` / `-layout`，替代已移除的运行时包
- 从客户端 inject 列表移除未使用的 `sessions` 服务

### 新增
- 版本守卫：声明 `dsh.compatibility.dshReleases` 与 `engines.dsh` 供市场做安装前兼容预检，并以 peerDependencies 约束相关 DSH 包；`chat` hook 不可用时优雅降级

## [0.2.2] - 2026-08-24

### 新增
- 关键字搜索：放大镜打开搜索框，回车逐处跳转（`n/N` 计数），Esc 或再次点击放大镜关闭
- 搜索范围切换：仅标题，或同时搜索用户消息与 AI 回复正文
- 对话内高亮：命中的关键字在对话中高亮，当前命中单独标亮并滚动到视口中上部
- 同一条消息内的多次命中都计入 `n/N`
- 无标题的回合也有独立的时间条目；所有组头显示该回合首行预览

### 变更
- 两种停靠的面板收起动画统一（沿侧栏/屏幕边缘裁切，移除阴影）
- 非激活的大纲组降到 0.6 不透明度

## [0.1.1] - 2026-08-17

### 变更
- 发布流程额外上传稳定命名的 tarball（`dsh-quick-toc.tgz`），让 `/releases/latest/download/` 始终指向最新版本

## [0.1.0] - 2026-08-17

### 新增
- 按回合分组的大纲：每条用户消息与其后续 AI 回复为一组，组头显示该组的结束时间
- 自动跟随高亮：对话视口内的回合在大纲中点亮，大纲自动滚动保持可见
- 平滑跳转：点击标题平滑滚动到对话中该标题的位置
- 左右停靠、拖拽调整大小、收起为边缘把手
- 滚动条随停靠方向（停靠左侧时在左，右侧时在右）
- 分页渲染：默认显示最新的若干组，向上滚动加载更早的组
- 自动「加载更早」：大纲到达顶部且已全部加载时，会点击对话自身的加载按钮
- 组头时间跳转：点击时间标签跳到该回合开头
- Markdown 感知标题：剥离 `**加粗**`、`*斜体*`、`` `代码` ``、`[链接](url)`、`~~删除线~~`
- 面板状态持久化（停靠边、位置、尺寸），并从旧键名自动迁移
- 对话无标题时自动隐藏；支持亮/暗主题

### 发布
- npm：`dsh-quick-toc@0.1.0`
- GitHub：`LyaxZ/dsh-quick-toc`，带自动发布流程（推送标签 → npm pack → release 资产）
